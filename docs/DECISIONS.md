# DECISIONS

> Any deviation from CLAUDE.md, and any significant technical choice, goes here with date and reason.

## 2026-07-02 — CLAUDE.md filename
Uploaded file was `claude.md.txt` wrapped in a markdown code fence with CRLF endings. Renamed to `CLAUDE.md`, stripped fence, normalised to LF. Content unchanged.

## 2026-07-02 — Tailwind v4 (CSS-first config)
Chose Tailwind v4 over v3. Its `@theme` layer maps semantic CSS variables straight into utilities (`bg-surface`, `text-fg`, `rounded-card`) with no colour duplication in a JS config. The stock palette and stock text sizes are disabled (`--color-*: initial`, `--text-*: initial` in `globals.css`), so raw colour utilities like `bg-blue-500` and ad-hoc sizes don't even exist — the no-raw-hex rule is structural, not just linted.

## 2026-07-02 — Radix UI primitives for interactive components
Select, Tabs, Modal (Dialog), Toast are built on unstyled Radix primitives for correct focus management, keyboard navigation, and ARIA. All visuals are ours via semantic tokens. Simpler components are hand-rolled. Note: the Select content is intentionally **not** portalled so it inherits the `data-theme` scope it was opened in (needed for the side-by-side preview and harmless in-app); Modal/Toast are portalled and accept `data-theme` pass-through when scoped theming is needed.

## 2026-07-02 — Fonts: self-hosted; General Sans is a drop-in slot
All fonts self-host (no CDN flicker, kiosk-friendly offline). Hanken Grotesk woff2 vendored from `@fontsource-variable/hanken-grotesk` into `src/fonts/`, loaded with `next/font/local`. Geist Mono via Vercel's `geist` package (bundles woff2). **General Sans could not be fetched this session**: it is not on npm, Fontshare/its CDN are blocked by the session network policy, and GitHub access is scoped to this repo only. It ships as a manual `@font-face` in `globals.css` pointing at `public/fonts/general-sans/GeneralSans-Variable.woff2` (see README there) so dropping the file in activates it with zero code change; until then headings use self-hosted Inter — CLAUDE.md's documented fallback ("Inter Display feel"). This is graceful: a missing font file 404s and falls back, whereas `next/font/local` would fail the build.

## 2026-07-02 — Semantic token set extended beyond CLAUDE.md's list
CLAUDE.md names 14 tokens; the shipped set adds: `--bg-hover`, `--border-strong` (inputs need a ≥3:1 boundary per WCAG 1.4.11; `--border-subtle` stays decorative), `--text-disabled` (explicitly non-AA, disabled controls only), `--text-on-accent`, `--accent-text` (links need ≥4.5:1, the solid accent is tuned for fills), `--*-subtle`/`--*-text` per status, `--overlay`, `--brand`, `--elevation-card/raised`, font/type-scale/radius primitives. Reasons: WCAG AA for every text/background pair forced the text/fill split; everything is documented in `design/tokens.md`.

## 2026-07-02 — Status text never sits on solid status colours
StatusPill/Badge use tinted `*-subtle` backgrounds with dark `*-text` — amber with white text can never reach 4.5:1, and the tinted style is calmer (Linear-like). Solid status colours are reserved for dots, icons, and the destructive button fill (`--critical` + white is verified AA).

## 2026-07-02 — Stage 1 scope: no backend wiring
Supabase, TanStack Query, Zustand, and Inngest are intentionally absent from `package.json`. Stage 1 is pure UI; adding them unconfigured would only create drift. They enter with Stage 2 (schema + RLS) per CLAUDE.md.

## 2026-07-02 — lucide-react for icons
One icon set, tree-shakeable, neutral style that suits the calm aesthetic. Icons always render with `aria-hidden` and inherit `currentColor` so they obey semantic tokens.

## 2026-07-02 — Contrast + raw-colour checks are scripts, not docs
`npm run check:contrast` parses `src/styles/tokens.css` (single source of truth) and fails below AA; its `--md` output is embedded in `design/tokens.md`, so the documented ratios are generated, never hand-typed. `npm run check:tokens` fails on any raw hex/rgb/hsl/oklch or stock Tailwind colour utility under `src/`. Both should run in CI when CI exists.

## 2026-07-02 (later) — "FOCT Premium Operations UI" visual upgrade (Stage 1.5)
Owner brief: the Stage 1 output read as a generic admin dashboard — too small, too text-heavy, not premium. Upgraded the visual system without changing the token architecture or the five CLAUDE.md themes:
- **Type scale up one notch across the board** (body 15→16, tables 13→14 minimum, page titles 24→36, plus a 72px `hero` size for the kiosk clock). All-caps micro-labels removed (table headers and eyebrows are now sentence case).
- **Deep sidebar rail** via a new `--sidebar-*` token family, tuned per theme (charcoal/navy/green-grey/umber), AA-verified like everything else. This is the single biggest "not-an-admin-template" move.
- **`--info` status family** (slate blue) added — the brief's status set needed an informational tone ("Ordered", "Requires Pro").
- **Radius/controls up**: cards 14→16px, controls 10→12px, buttons 36/44px, inputs 44px, table rows ~56px.
- The owner-supplied token names (background/foreground/card/muted/…, shadcn-style) were mapped onto the existing documented semantic set rather than renaming: same roles, and renaming would have destroyed the contrast-check/reference documentation for no visual gain. Mapping documented in design/tokens.md.

## 2026-07-02 (later) — Stage 1.5 demo screens ahead of Stage 2 backend
The visual brief required real screens (dashboard, kiosk, roster, timesheets, consumables, module access) to judge the design system operationally. Built them as static pages rendering exclusively from `src/lib/demo-data.ts` (Aurora on Collins cast) — no Supabase, no new modules beyond their packaging tiles. This front-runs Stage 3+ *layouts* only; every screen must be re-bound to real data and RLS in later stages. The kiosk PIN directory is an in-file demo map, clearly marked.

## 2026-07-02 (later) — Referenced design attachments not found
The brief referenced Figma/Octet/Google Stitch/Lovable design attachments "already provided in the project"; no such files exist in the repo. Direction was taken from CLAUDE.md's documented references (Apple HIG, Radix, Stripe/Linear calibre) and the brief's own style keywords. If those attachments exist elsewhere, add them to `design/` and we can tune against them.

## 2026-07-03 — TailAdmin-inspired restyle (owner reference)
Owner rejected the Stage 1.5 look and pointed at the TailAdmin Figma community file as the direction. The Figma link and TailAdmin's demo sites are unreachable from this environment's network policy, so the restyle works from TailAdmin's well-known visual system (Untitled-UI-style gray ramp, white sidebar, vivid indigo `#465FFF`, bold stat numbers with trend chips, soft pill badges):
- **Graphite retuned** from warm-sand/graphite-blue to cool gray-50 canvas + indigo `#465fff` accent, and made the app-shell theme for the design review. Note: CLAUDE.md forbids "harsh admin-template blue"; the owner's explicit reference overrides — the indigo passes AA with white (4.8:1) and `--accent-text` uses the deeper `#3641f5` for small text. CLAUDE.md's Graphite description ("warm off-white… graphite-blue") is superseded by this entry.
- **Light sidebar rail in all light themes** (white, hairline border, `--accent-subtle` active pill); Ink keeps its dark rail for kiosks/night mode. The dark-rail experiment from earlier today is retired.
- **MetricCard restructured to the TailAdmin arrangement** (icon well top-left, trend chip top-right, label + big value below). Metric values now use the display face with `tabular-nums` instead of Geist Mono — mono stays for tables, timestamps and IDs per CLAUDE.md; large stat numbers read as data-display, not tabular data.
- **MiniBarChart** added (dependency-free, tokens-only, sr-only table for a11y) — dashboard now shows actual-vs-rostered hours for the week, a genuinely useful chart, not decoration.
- Contrast suite extended to 37 pairs × 5 themes (adds the active-pill pair); all pass.

## 2026-07-03 (later) — v3 "reference build" restyle: ink + teal, uppercase labels, light kiosk
Owner shared screenshots of their preferred FOCT build (plus Deputy references) and asked to change the design again. Applied:
- **Graphite accent split**: `--accent` is now ink `#101828` (primary buttons, e.g. "Approve week"), `--accent-text` is FOCT-family teal `#0e7569` (links, live signals, focus ring). Active nav pill is a neutral `--accent-subtle` well with dark text. Indigo `#465fff` retired.
- **Uppercase micro-labels return** (stat-card labels, page eyebrows, table headers, sidebar group titles with 0.06–0.08em tracking) — matches the reference; supersedes the earlier "fewer all-caps" call.
- **Dashboard rebuilt to the reference composition**: hero card (building + LIVE + ticking LiveClock + on-site/open/late chips + kiosk CTA), weather card (static sample data until an integration exists), cleaning-progress donut (CSS conic-gradient), uppercase stat row, live activity feed, camera wall as a **locked Automation Pro card** (dark tiles, no functionality — CLAUDE.md forbids building camera analytics beyond a shell).
- **Timesheets rebuilt per reference**: per-shift rows with avatar chips, scheduled date/time, check in/out, hours progress bar (x/y h), variance pills (Missing red / ±h amber / On time green), status chips; "Approve week" ink button.
- **Kiosk is now light** (Deputy-style): default light theme, white keypad card, teal "Clock in and out with ease" pill, "Welcome, {name}" once a known PIN is entered. Ink theme remains available for night mode but is no longer the kiosk default.
- Sidebar regrouped into Core / Cleaning / Concierge / Automation with locked items per module packaging; top bar now has org + building switchers, a "Preview build" chip, and identity. New components: Avatar, LiveClock.

## 2026-07-03 (later) — Three palette variants for owner selection
Owner supplied three colour-palette references and asked for quick variants to choose from. Added three temporary review-only theme blocks to tokens.css (`option-analytics`, `option-blush`, `option-slate`), all AA-verified (37 pairs × 8 themes pass). The live preview artifact has a PALETTE switcher (Current / A / B / C). Once one is chosen it gets folded into Graphite as the default skin and all `option-*` blocks are deleted — they must not ship.

## 2026-07-03 (later) — Typography switched to the Roboto family
Owner direction: "Roboto family — light, ultralight, dark by category, or SF Mono." Implemented:
- **Roboto variable (100–900)** self-hosted for both display and body; hierarchy now comes from weight, not family: hero 200, display/title-1 300, title-2 400, title-3/labels/buttons 500, body 400. Semibold usages retuned to medium (Roboto 600+ reads heavy).
- **Roboto Mono variable** for all numerics/timestamps/IDs; `"SF Mono"` is next in the fallback stack so Apple hardware uses it natively — SF Mono itself is Apple-proprietary and cannot legally be bundled.
- Hanken Grotesk, Inter, and the Geist Mono package usage are retired from the stacks; the General Sans `@font-face` drop-in slot is removed (README left in `public/fonts/general-sans/` as history). CLAUDE.md's typography section (General Sans/Hanken/Geist) is superseded by this entry at owner direction.

## 2026-07-05 — Owner-approved font library + Mona Sans / DM Sans / Roboto Mono pairing
Owner supplied an approved font library (Finlandica, Mona Sans, Roboto, Radio Canada, Hubot Sans; reference set DM Sans, Satoshi, Inter, Manrope, General Sans) with standing rules: fonts only from this list for ALL work, big numbers big & thick, body line height 1.4–1.6. Written into CLAUDE.md → Typography (supersedes both the original spec and the Roboto-only pass from 2026-07-03). Shipping pairing: Mona Sans (headings + display numerals, 600–700 — the previous light-numeral ramp is inverted per the rule), DM Sans (body, 400/500), Roboto Mono (numerics, SF Mono fallback). Caption line height 18→19px to stay inside the band. Satoshi/General Sans remain unbundleable (Fontshare blocked); Finlandica/Radio Canada/Hubot Sans/Manrope are approved alternates available on npm.

## 2026-07-05 — Five more palette variants (D–H) from owner references
Owner found A/B/C (and the ink+teal default) "robotic, no human touch" and supplied five new references (Mangeo sunset brand, Booster violet+amber, Datify cobalt+blush, a navy/sky/gold scheme board, and a natural-greens CRM palette). Added five review-only themes, each with a warm/tinted canvas rather than sterile grey: `option-sunset` (cream + plum ink + burnt orange), `option-violet` (lilac + deep violet), `option-cobalt` (cool white + cobalt + blush wells), `option-nightfall` (navy ink + sky wells), `option-garden` (warm paper + charcoal + leaf green). All AA-verified (37 pairs × 13 themes). Same rule as A–C: the chosen one folds into the default, all other `option-*` blocks get deleted before Stage 2.

## 2026-07-06 — Bricolage Grotesque + Onest; Sunset becomes the review default
Owner: fonts still "automated machine fonts" — wants bold, big, clean, designer-recommended, and colours changed too. Actions:
- **Bricolage Grotesque** (display, weights to 800) added to the approved library and shipped for headings + all big numbers, including the kiosk clock (mono demoted to table timestamps/durations/IDs only). It is the closest free-font spirit to the Halvar Breitschrift energy in the owner's Mangeo reference.
- **Onest** (body) added and shipped — it appears by name in the owner's own Datify reference. Weight ramp now hero/display 800, title-1 700, title-2 700, title-3 650→600s, body 400/500.
- **Default theme for the review build switched from grey Graphite to `option-sunset`** (layout + AppShell) so the app no longer opens in the cool-grey look the owner reads as robotic. Final default still awaits the owner's A–H pick; graphite tokens remain untouched underneath.
- Mona Sans and DM Sans stay in the approved library as alternates; their font files remain vendored.

## 2026-07-06 (later) — Variant F (Cobalt) eliminated
Owner: "Delete F." `option-cobalt` removed from tokens.css, the contrast suite (now 37 pairs × 12 themes, all passing) and the preview switcher. Remaining candidates: A Analytics, B Blush, C Slate, D Sunset (current review default), E Violet, G Nightfall, H Garden.

## 2026-07-06 (later) — Three Google Stitch designs fitted as variants I / J / K
Owner supplied three complete Google Stitch HTML designs (Vibrant Glassmorphism, Soft Nature, Cyberpunk Dark) and asked to fit our software into them as closely as possible. Because every visual property in this system is a token, each design became a switchable theme block rather than a fork of the UI:
- **`option-nature` (I · Nature)** ← "Soft Nature": warm paper canvas `#f9f7f2`, sage/olive accent ramp (`#55684b` buttons, `#7d9070` brand), terracotta critical (`#b1502f`), and per-theme radius overrides (`--r-card: 1.75rem`, `--r-control: 1.25rem`) for the source's pillowy, extra-round look; soft green-tinted card shadow.
- **`option-cyber` (J · Cyber)** ← "Cyberpunk Dark": near-black canvas `#0a0a0c`, neon cyan accent `#00f2ff` with dark text-on-accent, purple `#bc13fe`-family brand/info, glow-style `--elevation-*` (cyan ring + bloom instead of drop shadow), a theme-scoped fixed radial cyan aurora on the canvas, and a per-theme `--font-stack-display` override to **Roboto Mono** — proving font voice is themeable per data-theme without touching components.
- **`option-glass` (K · Glass)** ← "Vibrant Glassmorphism": deep violet canvas `#241b3e` with a scoped 4-stop fixed mesh gradient (purple/pink/blue radials), tonal frosted-look surfaces, white primary buttons (`--accent: #f5f2ff`, dark text) and lavender link text.
Translation compromises (logged honestly): real `backdrop-filter` translucency is replaced by solid tonal equivalents so the WCAG checker's hex math stays valid; the sources' Quicksand/Poppins are not in the owner's approved library, so Nature/Glass keep Bricolage + Onest and Cyber uses Roboto Mono display via the token override. All AA-verified: 37 pairs × 15 themes pass. Same disposal rule as A–H: these are review-only and get deleted once the owner picks a winner. Candidate list is now A–E, G–K (ten).

## 2026-07-06 (later) — Nature & Glass promoted to finalist candidates, rebuilt to copy their Stitch sources in full
Owner uploaded refreshed Stitch packages for Soft Nature and Vibrant Glassmorphism and directed: these two are the finalists — copy the whole styling (tile anatomy, not just colour), keep the other variants untouched. Because tile anatomy now differs per theme, the token system gained three **structural hooks** with `:root` defaults so the other 13 themes render exactly as before:
- `--card-border` (new `border-cardline` on the Card primitive; defaults to `--border-subtle`)
- `--sidebar-active-bg` / `--sidebar-active-fg` (the active-nav pill; defaults alias the old accent-subtle treatment)
The contrast checker now merges `:root` defaults under every theme and verifies the pill pair as actually rendered (still 37 pairs × 15 themes, all AA).

**I · Nature (rebuilt):** borderless white cards floating on the design's exact sage shadow (`0 8px 30px rgb(125 144 112 / 0.08)`), cream rail on the canvas with a **solid sage active pill + white label**, pill-shaped controls/chips everywhere (`--r-control: 9999px`), cards 1.5rem. Accent sage `#657a59` (the design's `#7d9070` darkened to AA; full-strength kept as `--brand` for graphics). One rounded family: display switches to **Onest** per-theme (the source uses Quicksand everywhere; Quicksand isn't in the approved library).

**K · Glass (rebuilt):** REAL glassmorphism this time — the theme block keeps solid tonal equivalents as the AA-proof basis, and theme-scoped rules layer the actual look: violet `135deg` gradient + 4-radial mesh canvas, `white/6 + backdrop-blur(16px)` cards, `white/8` frosted rail, glass chip buttons (`bg-accent` → translucent white/12 with a white/20 border), glowing white/10 active pill, white/10 hairlines. Status shifted to the source's emerald/amber/rose/blue; `--text-on-accent` is now white, so glass's `--critical` deepened to `#d12f4f` to keep destructive buttons AA. Display face also Onest (source uses Poppins throughout).

Collateral fixes: large media tiles (dashboard camera wall, roster note/board tiles, select dropdown panel) were borrowing `rounded-control` and ballooned under Nature's pill controls — retagged `rounded-card` (correct semantics in every theme). The dashboard progress donut is now a true masked conic ring instead of a painted `bg-surface` hole, which the frosted glass cards were showing through.

## 2026-07-06 (later) — Data-viz chart tokens; every theme now colours its charts in its own palette
Owner: the 33% donut kept the same green/amber in every theme, and elements must blend to each palette across the whole app. Root cause: charts were wired to the STATUS ramp, which is deliberately shared across themes. Fix at the token level:
- New `--chart-1/2/3` (primary series / secondary / track) with `:root` defaults derived from the theme accent family (`--accent` / `--warning` / `--bg-hover`) — so ALL 15 themes retheme their charts automatically (Sunset gets a burnt-orange ring, Cyber a neon-cyan one, etc.). Mapped to `bg-chart-*` utilities; the dashboard donut + legend now consume them.
- **Nature**: `--chart-*` overridden to the design's sage ramp (`#657a59` / `#7d9070` / `#e5e1d3`); status family retuned INTO the palette — success is now the sage trio, info a stone teal (`#4e7d74`/`#e4efec`/`#3e685f`) — so pills, activity dots, meters, weather icons, the kiosk success screen and "Streaming"/"Requires Automation Pro" badges all read sage/terracotta, no off-palette green/blue anywhere. All AA-verified.
- **Glass**: `--chart-*` set to the Stitch source's blue+amber ring over the accent-subtle track.
The five permanent themes keep their CLAUDE.md-mandated shared status colours; their charts still retheme via the accent-derived defaults.

## 2026-07-06 (later) — Service Desk module: PRD accepted with conformance deltas; static preview shipped
Owner supplied a full Service Desk (Zendesk-style ticketing) brief plus plain-language additions: **anyone with the site link/QR can lodge on the per-site form** (created inside the org), and **multiple follower emails per ticket** who receive updates + the closure email. Stored verbatim-in-spirit at `docs/modules/SERVICE_DESK_PRD.md` with a BINDING conformance table where the brief conflicted with platform rules (fonts → approved library; Radix-step tokens → semantic tokens; Org→Client→Site hierarchy → organisations×buildings; Twilio/Resend hardcoding → Integrations Framework adapters; Expo/PowerSync → flagged as NEW stack decisions needing owner sign-off; Next.js 16 → repo's pinned version; "Aurora Melbourne Central" → fixed Aurora on Collins seed).
Shipped now (static, consistent with Stage 1.x): `/service-desk` queue (metric row, saved views with counts, status filter, SLA countdown/breach/met chips, priority pills, ticket table, detail drawer with before/after photo pair, proof-PDF row, CSAT chip, follower chips, internal-note-highlighted activity timeline) and `/service-desk/new` (name-from-dropdown, multi-location rows, category, camera-first photo zone, priority segmented control, follower email chips, public QR/link side card). Nav entry under Core; Module access card added; demo cast extended with Concierge Collective concierges (Amelia Ng, Oliver Reyes). Data in `src/lib/service-desk-data.ts`.
Backend stages 1–6 (schema+RLS → intake → queue → notifications → PDF/close → SLA/analytics) remain GATED on platform Stage 2 and the owner's answers to the PRD's open questions (mobile stack, SLA defaults, WhatsApp template copy, public-form exposure).

## 2026-07-06 (later) — Service Desk made functional (client-side store, Stage-2-swappable)
Owner: "make functionality for this." The real backend (Supabase/RLS, Inngest fan-out, WhatsApp, PDF) still needs the Stage 2 platform foundation and provisioned services, so the module got a **fully working client-side implementation** instead of dead screens: a Zustand store (`src/lib/service-desk-store.ts` — Zustand is the CLAUDE.md-approved client-state library) with localStorage persistence (`foct-sd-demo-v1`, quota-safe wrapper, `skipHydration` + post-mount rehydrate for SSR safety) drives the entire ticket lifecycle:
- **Create**: the intake form validates (name, level, category, ≥1 photo), compresses real camera/gallery photos client-side (`src/lib/compress-image.ts`, canvas → ≤900px JPEG data URLs — the same output Stage 2 uploads to Supabase Storage), generates the next `SD-AUR-2607-NNNN` ref, appends created+notify timeline events, and lands the ticket at the top of the queue.
- **Work**: assign (select in drawer), Attend → In progress, internal notes (highlighted, "hidden from concierge"), follower emails add/remove on live tickets, **Close with proof** (cleaner name + ≥1 after photo required + optional note → Resolved, closure event lists lodger + follower count, proof-PDF ref attached), Reopen, and a CSAT 👍/👎 simulation on resolved tickets.
- **Queue**: saved-view counts computed live from the store; real photo thumbnails render in the before/after tiles; "Copy intake link" uses the clipboard; "Reset demo" restores seeds.
Verified end-to-end with a Playwright run (validation → lodge with a real JPEG → attend → note → close with after photo → CSAT → **persists across reload**). Notifications/PDF remain simulated timeline events labelled "(simulated in preview)" — honest until Stage 4/5. The screens talk only to the store's action API, so Stage 2 swaps the store internals for Supabase without touching the UI.

## 2026-07-06 (later) — Stage 1.5 Theme Builder (owner-directed scope change, approved plan)
Owner inserted a new stage before module prioritisation: a professional Appearance/Design settings area. Scope: (1) Themes tab — every theme in the codebase (all 15, values untouched) as live preview cards, applyable per building; (2) custom theme creator — duplicate any theme, edit via hex inputs (canvas, surface, text primary/secondary, accent, success, warning, critical; remaining tokens auto-derived), live preview, WCAG AA validation on save with warnings; custom themes saved per org/building alongside built-ins; (3) Typography tab — pickers for the display/body/mono slots from the approved-library fonts vendored in the repo (Bricolage, Onest, Mona Sans, DM Sans, Inter, Roboto, Roboto Mono — Hanken excluded, retired from the library) plus custom woff2 upload (FontFace API now, org-scoped Supabase Storage path recorded for Stage 2); (4) persistence targets `themes` / `building_theme_assignments` — since the platform has no Supabase yet, the migration SQL (+RLS +pgTAP isolation test) is AUTHORED in `supabase/migrations/` and executes at Stage 2, while a Zustand store adapter with the identical row shape persists locally so the feature is live today (same swap pattern as Service Desk).
CLAUDE.md updated additively to reflect today's reality (Service Desk in the module list; current 15-theme set with Nature/Glass finalists; structural + chart token hooks; Theme Builder). Also this stage: contrast math extracted to a shared module consumed by BOTH scripts/check-contrast.mjs and the UI validator so they cannot drift.
After this stage completes: present the module registry as a checklist for the owner's v1 priority order — no functionality coding until the order is confirmed.

## 2026-07-07 — Stage 1.5 Theme Builder SHIPPED
Appearance settings live at `/settings/appearance` (Settings group in the nav). What shipped:
- **Themes tab**: all 15 built-ins as live `data-theme` preview cards (real themed subtrees, not images) with Apply-per-building and Duplicate; custom themes listed alongside with AA status badges, Edit and Delete.
- **Custom theme creator**: 8 authored tokens (canvas, surface, text primary/secondary, accent, success, warning, critical) via paired colour-picker + hex inputs; every other token (subtles, hovers, borders, sidebar, chart, disabled ramps) derived in `src/lib/theme-builder.ts` and contrast-nudged; the 8 authored values are respected verbatim and validated — the save flow warns with the exact failing pairs/ratios (warn-modal allows save-anyway). Validation reuses the SAME pair list + math as the build gate via shared `src/lib/wcag.js` + `src/lib/wcag-pairs.js` (check-contrast.mjs now imports them — no drift possible).
- **Application model**: custom themes render as `data-theme={base}` + `data-custom-theme={slug}` with injected CSS overriding colour vars — so a custom inherits its base's radius/type voice/scoped decorations (glass mesh, nature pill controls) while recolouring. AppShell reads the building assignment from the store; SSR renders the default and the persisted assignment applies post-hydration.
- **Typography tab**: display/body/mono slot pickers limited to approved-library fonts vendored in the repo (Bricolage, Onest, Mona Sans, DM Sans, Inter, Roboto, Roboto Mono — alternates load with `preload:false`); custom `.woff2` upload via FileReader + @font-face now, org-scoped Storage path recorded for Stage 2; slot overrides apply live via inline `--font-stack-*` vars (beating per-theme font overrides by design).
- **Persistence**: `src/lib/theme-store.ts` (Zustand, `foct-theme-builder-v1`) with row shapes mirroring `supabase/migrations/0001_theme_engine.sql` — authored this stage with RLS in-file + pgTAP isolation test (`supabase/tests/`), executing at Stage 2 (depends on core tables/helpers; documented in the migration header).
- **Tooling**: `npm run export:themes` regenerates `src/lib/theme-tokens.json` (duplication baselines) from tokens.css; check-tokens allowlists the two theme-data files that legitimately contain hex.
Verified: Playwright e2e — apply Nature (app rethemes) → duplicate → break text-primary (#cccccc) → AA warnings listed → save blocked behind warn-modal → fix → all pairs pass → save → apply custom (renders over Nature base) → switch display font to Mona Sans (live) → upload real woff2 → reload → assignment/theme/fonts all persist. All gates green.

## 2026-07-07 — Final palette decision: keep A/B/C/D, NATURE is the default
Owner: "For pallets: A,B,C,D,I and I being the default theme." Applied:
- **`option-nature` is the product default** — layout `data-theme`, `DEFAULT_THEME` in the theme store, and the review build all open in Nature. The graphite `:root` block stays as the CSS-level fallback for unthemed contexts; the *product* default is Nature via assignment, which also keeps the Theme Builder's "default = just another assignable theme" model clean.
- **Kept as selectable built-ins:** Analytics (A), Blush (B), Slate (C), Sunset (D) + the five originals (Graphite, Harbour, Eucalypt, Sandstone, Ink) → ten themes total.
- **Deleted:** option-violet, option-nightfall, option-garden, option-cyber, option-glass (tokens.css blocks incl. Glass's scoped translucency CSS, registry entries, checker list). `theme-tokens.json` regenerated (10 themes); contrast gate now 37 pairs × 10 themes, all AA. AppShell falls back to the default for any stale persisted slug pointing at a deleted theme.
- CLAUDE.md theme-set section updated to the final registry; slugs unchanged (renaming would break persisted building assignments — cosmetic renames can happen at Stage 2 with a data migration).

## 2026-07-07 — Stage 2 platform foundation: authored + locally verified; PWA-first kiosk locked in
Owner supplied the Supabase project (Sydney) + publishable key and said "start Stage 2". Constraint discovered: this build environment's egress proxy blocks `*.supabase.co`, so the project cannot be reached from here. Response: migrations were validated against a REAL local Postgres 16 cluster instead, and the owner applies them with one paste (supabase/README.md):
- `0000_platform_foundation.sql`: every CLAUDE.md core table, `app.*` policy helpers (is_org_member, can_access_building, manages_building, has_role, current_org_id…), RLS on all 17 tables, grants. `0001_theme_engine.sql` verified to run on top (one column-name bug in its bta_read policy caught and fixed by the local run). `seed.sql`: full Aurora on Collins cast + module packaging + contracts + 10 built-in theme rows + Nature assignment + a rival org purely for isolation proof.
- `tests/isolation_check.sql`: the CLAUDE.md isolation acceptance tests as plain SQL (23 assertions — rival org sees nothing; concierge never sees cleaning staff/contracts; owner org can't browse contractor staff; cross-org theme reads/writes blocked; built-ins visible to all). ALL PASS locally with RLS enforced via `set role authenticated` + JWT-claim emulation. Runs identically in the Supabase SQL editor post-apply.
- App wiring: `@supabase/supabase-js` + `@tanstack/react-query` installed; `src/lib/supabase.ts` env-driven browser client with `isSupabaseConfigured` so screens gracefully keep local demo stores where env/network is absent; keys in `.env.local` (gitignored), template in `.env.example`.
- **Kiosk decision (owner, same day): PWA-first confirmed** — owner's condition was offline capability; the offline queue design (service worker + IndexedDB + background sync) satisfies it; native Expo wrap stays an additive option.
Also this date: **final palette** (Nature default; A/B/C/D kept; 5 variants deleted) — see prior entry.

## 2026-07-07 — Owner's Claude Design "Building Support Tickets" handoff: SAME module, own themed mobile surface + new "support" theme
Owner uploaded their Claude Design handoff (Mobile App.dc.html + a desktop set: login, 3 dashboards, ticket list/detail, create, billing, PDF preview, admin settings — same palette) and asked: integrate into Service Desk, separate module, or a theme? Decision (recommended + implemented): **not a separate module** — one ticket data model; the design became the **mobile surface of the existing Service Desk** at `/support` (own layout + own theme, kiosk-precedent), wired to the SAME store as the desktop queue. E2E-proven: a ticket created on the phone flow appears on the desktop queue.
- **`support` theme** added to tokens.css from the design's exact palette (ink #0E0F11 on white, signal-red #DE192A accent — 4.91:1 with white so it survived AA untouched; nudges: muted text #6A737F→#59626d, success text →#157a4e, warning text →#8a5800, border-strong →#7a8492). Selectable in the Theme Builder, so the owner's "or add one theme based on this" is delivered too. 37 pairs × 11 themes AA.
- **Screens** (`/support`, `/support/new`, `/support/jobs`): concierge home (greeting, big red Create, My tickets count, Today list), 2-step tap-only create (quick-level grid + all-levels select, area chips + custom, icon category grid from sd categories, priority segmented, camera photos ≥1 enforced, quick-note chips, success screen with real ref), cleaner My jobs (Today/All/Completed, UP NEXT card) and job detail (before photos, quick remarks, Attend, after-photo-gated Mark completed, completed state with attended/completed times), empty state. Status labels speak the design's vocabulary (Assigned/Completed) over the same store statuses.
- **Fonts substituted per approved library**: Archivo→Bricolage Grotesque (800), Hanken Grotesk→Onest, Geist Mono→Roboto Mono. "Subzero" branding → FOCT BuildingOps + Aurora cast (Amelia Ng concierge, Marcus Chen cleaner).
- Desktop screens from the handoff = future reskin option via the `support` theme; not rebuilt (the desktop queue already exists).

## 2026-07-07 — Stage 3 (part 1): Service Desk backend authored + locally verified; live-mode data layer shipped behind a flag
Owner confirmed Stage 2 applied ("applied, 23 ok"). Delivered the ticket-system backend:
- **`0002_service_desk.sql`**: sd_tickets/locations/photos/events(immutable, `internal` flag)/followers, sd_categories/site_staff/sla_policies, sd_intake_tokens + `sd_lodge_ticket` SECURITY DEFINER RPC (the public QR/link path: anon-executable, token-gated, validates payload, ≤5 photos as compressed data URLs, per-building monthly ref sequence `SD-{CODE}-{YYMM}-{seq}` via `app.sd_next_ref`). RLS on everything: tickets visible to every org serving the building; INTERNAL notes readable/writable ONLY by the servicing org (not concierge, not even the building owner); intake tokens readable by building managers only (RPC reads as definer); ref counters RPC-only.
- **`seed_service_desk.sql`**: global category set, Aurora staff registry (drives dropdowns + routing), FOCT SLA defaults (urgent 15m/2h · high 45m/4h · normal 2h/8h · low 4h/24h — PRD open question answered by shipping defaults, owner can adjust in-table), demo intake token.
- **`tests/sd_isolation_check.sql`**: 15 assertions, ALL PASS on the local PG16 mirror — incl. anon lodges via valid token / invalid token rejected / anon has no direct table read / concierge cannot read or write internal notes / owner org cannot read internal notes / rival org sees+changes nothing.
- **App live mode** (`NEXT_PUBLIC_SD_LIVE=1`): `src/lib/sd-supabase.ts` maps sd_* rows to the exact SdTicketLive shapes the screens consume; store actions mirror to Supabase fire-and-forget; createTicket lodges via the intake RPC then refetches so the server ref becomes truth; `/sign-in` (email+password) added because RLS reads/mutations need a session — the public intake does not. Flag off by default; local demo store remains the e2e-tested path. **Live mode is compile-verified only in this environment (proxy blocks *.supabase.co) — owner smoke-tests on their machine.**
Remaining Stage 3 scope (next): notifications via the adapter framework (Resend email first), Storage-bucket photos for authed uploads, proof-PDF job, Realtime queue updates, concierge desk surface polish.

## 2026-07-07 — 0003_anon_hardening: strip Supabase's default anon grants
Owner's run of sd_isolation_check on the live project failed ONE assertion: "anon can read tickets directly". Root cause: Supabase projects auto-grant table privileges to the `anon` API role via ALTER DEFAULT PRIVILEGES — a project default the local validation cluster doesn't have. **No data was exposed** (RLS returned zero rows; the test demands a privilege error, stricter than the actual behaviour), but least privilege wins: `0003_anon_hardening.sql` revokes ALL table/sequence access from `anon` (and the default privileges for future tables), leaving anon exactly one capability — executing the token-gated `sd_lodge_ticket` RPC. Verified locally by EMULATING the Supabase condition (grant-all to anon first) and re-running the suite: 15/15 pass. APPLY_STAGE3 regenerated to include 0003; owner re-pastes it (idempotent) and re-runs the test.

## 2026-07-07 — Migrations made genuinely idempotent (drop-and-recreate policies/triggers)
Owner's re-paste of APPLY_STAGE3 failed with `policy "sd_categories_read" already exists` — I had claimed idempotency but only tables used `if not exists`; Postgres has no `create policy if not exists`, and I never double-ran the bundles. Fixed: every `create policy` (50 across 0000/0001/0002) and both triggers are now preceded by `drop policy/trigger if exists`. Both APPLY bundles regenerated and **verified by double-run on a fresh local DB** (STAGE2 ×2, anon-grant emulation, STAGE3 ×2 → 23/23 + 15/15 assertions pass). Process note recorded: "idempotent" claims require a double-run proof before shipping.

## 2026-07-10 — Typography overhaul: system font stack for UI text + Barlow DIN-style numerals (owner direction)
Owner directed the type system toward Power BI (Segoe UI text, DIN numbers) and Zendesk/Freshdesk (native system stack). Implemented:
- **UI text** (`--font-stack-display` / `--font-stack-body`): the Zendesk stack verbatim — `system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen-Sans, Ubuntu, Cantarell, "Helvetica Neue", Arial, sans-serif`. Renders SF on Apple, Segoe on Windows, Roboto on Android — zero font downloads for text.
- **Numbers** (`--font-stack-numeric`, new `font-numeric` utility): **Barlow** 600/700/800 vendored from @fontsource — the closest free DIN-alike (D-DIN/DIN 2014 aren't on npm; Segoe UI, SF and DIN are proprietary and must NEVER be bundled). Stack falls through to "DIN Alternate" (macOS) then Segoe/system. Applied to metric-card values, live-clock hero, dashboard hero stats/donut %/hours, with tabular-nums.
- Nature's Onest display override removed so the default theme uses the system stack too; Bricolage/Onest/etc. remain vendored Theme Builder alternates (`FONT_OPTIONS` gains "System UI (SF / Segoe)" + "Barlow (DIN-style numerals)"; `DEFAULT_SLOTS` now system-ui/system-ui/roboto-mono). Roboto Mono stays timestamps/IDs only.
- CLAUDE.md Typography section rewritten as the standing rule. Caveat: screenshots from this Linux container show the Linux fallback for system-ui; SF/Segoe appear on the owner's devices. Gates: 37 AA pairs × 11 themes, no-raw-colours, tsc, build — all pass.

## 2026-07-10 — Scope module + Subzero theme (owner's "Aurora Scope Explorer" upload)
Owner uploaded their standalone "Aurora Scope & Roster — Subzero" HTML and directed: add it as a portal module named **Scope**, and add its red/black look as one more theme (Nature stays default). Delivered:
- **`/scope` module** (Cleaning section of the nav): the five views ported to React — Overview (hour bars per entity, frequency mix, contract watch-list, night security, gate & door PM), Scope explorer (entity pills incl. the dashed optional OC2, per-frequency filter chips, zone task cards), Weekly roster (positions × week with expandable shift drawers, TIMES TBA badges, day totals), Day gantt (weekday/weekend segmented, 05–20h axis, dashed TBA bars with hover tooltips, separate 21:00→09:00 night-security band) and Periodic planner (12-month due-dot grid: quarterly Mar/Jun/Sep/Dec, bi-annual Apr/Oct, annual staggered; BILLABLE EXTRA flags). Dataset verbatim in `src/lib/scope-data.ts` and reconciles to the source's acceptance numbers (393.0 h/wk, 222+16 items, 60.0/46.5 day totals) — but renamed to the demo cast ("Aurora on Collins (AUR)", "FOCT Cleaning — Service Agreement") per the no-real-names rule.
- **Token compliance**: the design's 11-step red frequency ramp is NOT hardcoded — it's `color-mix(in srgb, var(--critical) N%, var(--bg-surface))`, and bars/gantt use `--chart-1/3`, so the whole module re-tints under every theme (verified by screenshot under Nature and Subzero). Masthead panel uses the sidebar token family. `color-mix(in srgb, …)` is the sanctioned way to derive ramp steps in module CSS (oklab/oklch literals stay forbidden by check-tokens).
- **`subzero` theme**: 12th tokens.css block from the design's dark palette (canvas #0B0C0E, surface #16181C, red #DE192A with white text 4.91:1, red sidebar active pill). Registered in BUILTIN_THEMES (dark), exported to theme-tokens.json, added to the contrast gate — 37 pairs × 12 themes AA on first pass.

## 2026-07-10 — Owner clarification: Scope belongs to the Service Desk suite
Correction to the entry above: Scope is NOT a Cleaning-section module. The owner's "Subzero" design set is one product family attached to the Service Desk — the phone screens are the PHONE APP (`/support` mobile surface) and the Scope explorer is the ADMIN DASHBOARD VIEW. Nav entry moved from Cleaning to Core, directly under Service desk. The `subzero` theme addition and Nature-stays-default are unchanged.

## 2026-07-10 — Roadmap wave 1 shipped in the approved order (owner: "Do in your above order")
All five items from docs/PRODUCT_ROADMAP.md's recommended order, each functional client-side on the established store-first pattern (Supabase swap at each module's backend stage):
1. **Flow #1 complete**: new `attendance-store` — the kiosk writes real check-in/out events (wrong-PIN + double-punch guards); roster statuses, dashboard signals, missed-check-in alerts (15-min grace) and weekly timesheets with variance/approve/CSV ALL derive from events vs rostered shifts. Roster gains Add-shift; dashboard hero/metrics/activity/week-chart went live (page split into a client component). Today's demo cast anchors to the clock so the status mix shows at any hour.
2. **Portfolio** (`/portfolio`, top of nav): exception-first head-office triage; Aurora's signals live from the attendance + SD stores, 7 seeded sister buildings; tile drill-down to the dashboard.
3. **Calendar** (`/calendar`, unlocked): periodic works auto-generated from the Scope dataset (planner cadence rules), seeded building events, category filters, day drawer, persisted Add-job.
4. **Scope Quote studio** (6th Scope tab): editable areas/rates → calculated vs contract hours per entity with under-scoped flags, machine fleet sized from m², award-rate weekly/annual pricing with on-costs + margin.
5. **Concierge desk** (`/concierge`, unlocked): tickets lodge into the SHARED SD store (e2e cross-surface proof), parcels register, org-private handover notes.
All gates green (tokens, 37×12 contrast, tsc, build, Playwright e2e). Backend note: attendance/calendar/concierge tables + RLS follow the Stage-2/3 migration pattern when the owner calls for them.

## 2026-07-10 — Dashboard goes fully live + calendar email reminders (owner request)
- Dashboard: Open-Meteo live weather (demo fallback, "live" tag), "Due at the building" (today + next-7-days from the shared calendar), org-private Team handover notes (new handover-store), Service desk open-by-status chart. Everything on the dashboard now derives from stores or live APIs — no static counters remain.
- Calendar: optional email reminder per job (any address; on-the-day/1d/2d/1wk lead). Reminders queue visibly (calendar outbox + day drawer); actual SMTP delivery goes through the integrations email adapter (Resend default) when the calendar backend stage lands — needs the owner's Resend API key.
- Confirmed to owner: Service Desk desktop+phone and the kiosk→timesheets loop were already fully functional; Android tablets just open /kiosk in a browser (PWA path per the 2026-07-07 decision).

## 2026-07-10 — Ticketing import step 1: offline sync engine (owner approved "functionality theirs, skeleton+skin ours")
Plan in docs/modules/TICKETING_IMPORT_PLAN.md approved verbatim. Step 1 shipped: `src/lib/sd-offline.ts` (IndexedDB outbox + ordered idempotent replay, auto-sync on online/60s/refocus + manual) now carries ALL live-mode SD mutations; SyncPill on desk + phone. Offline queue drains against the existing sd_* backend — no schema change yet. NEXT: 0004 migration (offline-ref renumber trigger, billing fields), PDF+email report port, billing lock screen, insights charts. Live-mode offline behaviour needs the owner's on-machine smoke test (env constraint unchanged).

## 2026-07-10 — Halo theme (owner's DeGirum Cloud reference): "platform clean"
Owner shared DeGirum Cloud Platform screenshots asking for a cleaner portal look. Delivered as the 13th built-in theme `option-halo` rather than mutating Nature: gallery-white canvas (#fbfbfc/#fff), hairline #ececee borders, near-black ink rail (#0b0c0e) and ink buttons, pill controls (--r-control 9999px), 1rem cards, whisper shadows, teal reserved for focus/info/brand. 37 AA pairs × 13 themes. Nature REMAINS default pending the owner's call after comparing.

## 2026-07-10 — Readability pass (owner: "simplicity + good row heights for analytics numbers")
Theme-independent structural rhythm, applied in the shared components so every theme benefits: table rows now 60px min with 24px gutters (h-12 headers, align-middle), metric cards get a label→number→context rhythm with roomier gaps and body-sm context, card insets unified at 24px, page canvas padding up (py-10 / lg:px-12), page headers settle with pb-10. Verified on timesheets in Nature + Halo.

## 2026-07-12 — Above-the-fold pass: compact Scope header + collapsible sidebar (owner: "less is more")
- Scope masthead retired: slim eyebrow/title header with the agreement note behind a "about this agreement" disclosure; stats sit in flow (flatter tiles). "Where the hours go" moved from below the fold to y≈434 at 1440×900 — data owns the first screen.
- Sidebar collapses to a 76px icon rail (PanelLeft toggle, tooltips on icons, state persists per device via localStorage `foct-sidebar-collapsed`).
- The page-by-page density pass continues (dashboard/roster next); principle recorded: content above the fold, one accent per view, disclosures over paragraphs.

## 2026-07-12 — Part B: app.can() unified authorisation (owner-approved plan, conditions met)
- `0004_authz_can.sql`: app.can(user, org, building, module, action) — SECURITY DEFINER, null-skippable clauses, fails closed, super-admin bypass matching is_super_admin. 18 core-table policies rerouted 1:1; sd_* stay on the proven helpers; users_read/organisations_read keep bespoke name-visibility; audit_insert unchanged (no super bypass by design). public.can RPC wrapper answers self-queries only.
- Condition 1 (both outputs pasted): BEFORE 38/38 ok, AFTER 38/38 ok on fresh DBs; APPLY_STAGE2 double-run with 0 errors both times (idempotent).
- Condition 2: APPLY_STAGE2 regenerated with a "WHAT CHANGED" banner — everything above it byte-identical to the applied copy; only 0004 below.
- Condition 3: authz.ts demo stub gated behind NEXT_PUBLIC_AUTHZ_DEMO_ALLOW_ALL=1 (default off, loud red console warning when on, fails closed otherwise).
- docs/ARCHITECTURE.md added (mermaid ERD + permission-flow walkthrough).

## 2026-07-13 — Live anon-hardening RESOLVED
Root cause confirmed: the owner's original APPLY_STAGE3 paste aborted at the 42710 policy error BEFORE reaching the 0003 hardening section, so anon table grants survived on the live project (RLS still returned zero rows — no data exposure). Fixed with a self-contained revoke-all + default-privileges block pasted directly; live verification query now shows anon holding EXACTLY one grant (integration_providers SELECT, grantor postgres). Owner to re-run both isolation suites for the green confirmation (23 + 15).

## 2026-07-14 — Stage 4: Integrations framework (dynamic, GUI-configured providers)
Owner approved the six-part plan (interfaces + 7 adapters, catalogue JSON Schemas, admin GUI, notify(), switch flow, test page) built on the existing app.can()/RLS model. Key decisions:
- **No brand SDKs anywhere.** Every adapter is plain `fetch()` against the provider's REST API (`src/lib/integrations/`), so "no feature code imports a brand SDK" holds in the strongest form — the dependency tree has zero provider packages. AWS SigV4 is a 60-line dependency-free signer (`email/sigv4.ts`, node:crypto).
- **Secrets architecture.** Secret fields (flagged `x-secret` in each catalogue JSON Schema) go to Supabase Vault via the SECURITY DEFINER RPC `integration_credential_save`; non-secret fields live in `integration_credentials.config`. Reveal (`integration_secret_reveal`) is EXECUTE-granted to service_role ONLY — the client can never read a secret back; the GUI shows the masked string and offers replace-only. Replace-only is enforced in the DB too: a BEFORE UPDATE trigger rejects changes to org/building/provider/secret_ref.
- **One active credential per org × building-scope × category** (partial unique index). `integration_credential_activate` requires a recorded passing test, deactivates-but-keeps the previous credential, and every save/activate/deactivate lands in audit_logs via trigger. `notification_log` records which provider handled every send (recipient stored masked); INSERT is service_role-only.
- **Pre-save tests are transient.** "Test connection" posts form values to `/api/integrations/test`, which calls the adapter's `verifyCredentials` and never persists anything; the save RPC then records the client-asserted result. The DB cannot verify an external API itself, so the activation gate is a UX/process control, not a security boundary (the security boundary is org-admin + RLS). Post-save tests re-run server-side from Vault and are stamped by service_role.
- **Vault on the local mirror.** Plain PG16 has no supabase_vault, so `tests/local_prelude.sql` ships a plaintext-locally shim of `vault.create_secret`/`vault.decrypted_secrets`; 0005 refuses to run if neither the extension nor the shim is present. Proven: full chain + double-run idempotency + APPLY bundle on a live-state mirror; 23/15/19 isolation assertions green.
- **Demo mode mirrors the backend honestly**: the GUI's localStorage store keeps only masked values + non-secret config — typed secrets are used for the simulated test then dropped. Live mode (NEXT_PUBLIC_INTEGRATIONS_LIVE=1 + SUPABASE_SECRET_KEY) drives the RPCs and API routes. Added dev deps: `server-only` (build-time guard on secret-handling modules), `playwright` (e2e).

### Provider API versions implemented (verified against official docs 2026-07-14)
| Provider | API / version | Send endpoint | Auth | Verify (no send) |
|---|---|---|---|---|
| Resend | REST, unversioned | POST api.resend.com/emails | Bearer | GET /domains |
| Postmark | REST, unversioned | POST api.postmarkapp.com/email | X-Postmark-Server-Token | GET /server |
| SendGrid | v3 | POST api.sendgrid.com/v3/mail/send (202, id in X-Message-Id header) | Bearer | GET /v3/scopes (asserts mail.send) |
| AWS SES | v2 (2019-09-27) | POST email.{region}.amazonaws.com/v2/email/outbound-emails | SigV4 (service "ses") | GET /v2/email/account (also flags sandbox) |
| Twilio | 2010-04-01 | POST /Accounts/{sid}/Messages.json (form-encoded, 201) | Basic sid:token | GET /Accounts/{sid}.json |
| MessageMedia | v1 | POST api.messagemedia.com/v1/messages (202, delivery_report:true) | Basic key:secret | GET /v1/delivery_reports |
| ClickSend | v3 | POST rest.clicksend.com/v3/sms/send (always HTTP 200 — check data.messages[].status) | Basic user:apikey | GET /v3/account |

AU note recorded for the owner: from 1 July 2026 ACMA's SMS Sender ID Register requires ALPHANUMERIC sender IDs to Australian numbers to be registered with the provider (Twilio/Sinch MessageMedia/ClickSend flows) or messages get labelled "Unverified" — dedicated numbers are the alternative and are required for two-way SMS.

## 2026-07-15 — UX wave: big create modals, recurring calendar, one colour language (owner direction, verbatim tasks)
1. **Create forms are BIG CENTRED MODALS, not side panels.** ModalContent gained `size="lg"` (46rem, scrolls internally). Calendar "Add event" and the desk's "Raise ticket" now open as large dialogs; `/service-desk/new` remains as the QR/public-link page, sharing the extracted `RaiseTicketForm` component. Side drawers stay for DETAIL views (day drawer, ticket drawer) — the rule is: reading slides in, creating pops up.
2. **Recurring calendar events** (calendar-store v2, same persist key — old data still loads): `CalSeries` with daily/weekly/fortnightly/monthly repeats, weekday picker, optional until-date, start/finish times, contact name + tap-to-call phone, and a new `maintenance` category. **Role-scoped visibility** ("everyone" or any of admin/BM/cleaning/concierge) and **admin-locked events** nobody else can change/remove — enforced in the store helpers (`visibleTo`/`canModify`) exactly as the future RLS will. A "Viewing as" selector stands in for real auth until the calendar backend stage. Dashboard due-list expands series too.
3. **The phone surface follows the PORTAL theme.** `/support` no longer pins the red `support` theme; it resolves the active Theme Builder assignment like AppShell (Nature olive by default). This supersedes the 2026-07-10 decision that gave the mobile surface its own red look — owner: "color and theme should follow the theme and color of web portal". The `support` token block stays in tokens.css (kiosk-style standalone theme, still selectable later).
Verified: 16/16 Playwright assertions (modal geometry, weekly expansion count, BM blindness to scoped events, cleaning's lock refusal, desk modal contents, computed `--accent` on /support = Nature's #657a59) · tokens ✓ · tsc ✓ · build ✓ (26 routes).

## 2026-07-15 — Ops wave 2: roster fixes, payroll-grade approvals, consumables ordering (owner tasks)
- **Roster timeline fixed**: the axis window now derives from the day's real shifts/check-ins (the old fixed 05:00–14:00 axis overflowed whenever the clock-anchored demo cast ran later); AttendanceTimeline additionally clamps every bar into its track and labels every 2nd hour on wide windows — bars can never escape the card again.
- **Add shift is a big modal and can create a CLEANER inline**: name + unique 4-digit kiosk PIN validated in `addStaff` (attendance-store); new cleaners persist (`customStaff`), register into the module directory (kiosk PIN lookup + derivations see them immediately) and survive the daily reseed.
- **Timesheets approve like payroll, not a rubber stamp**: per-row "Review & approve" opens a big modal with the per-day breakdown (rostered vs kiosk in/out vs actual), an editable hours-to-payroll figure (Use actual / Use rostered shortcuts) and a remarks field. Approvals persist `approvalMeta` (week, note, approvedHours); rows show "Approved · adjusted" + the remark; CSV export gains approved_h + remarks columns. "Approve all ready" stays for the clean cases.
- **Consumables ordering is real**: new `consumables-store` with a per-building ALLOWED catalogue (12 seeded items) and live orders. "New order" (big modal) shows icon-tile items with quantity steppers plus an "Other — custom item" line (flagged Custom in the queue); Approve/Decline now change order state; "Allowed list" modal lets the admin tick items on/off and add new catalogue entries — de-listed items disappear from the order form. Item "images" are design-system icon tiles per category, not stock photos.
Verified: 15/15 Playwright assertions (zero timeline overflows measured geometrically, duplicate-PIN rejection, adjusted-approval persistence, custom-item flag, allowed-list gating) · tokens ✓ · contrast ✓ · tsc ✓ · build ✓.

## 2026-07-17 — Ops wave 3: event date ranges, shift durations, per-shift corrections, consumables search + thumbnails (owner direction, verbatim tasks)
1. **Calendar events carry start + finish dates and more frequencies.** For one-off events the finish date makes a MULTI-DAY SPAN (the event appears on every day; store expands via `manualEventsForMonth`, occurrences share a `spanId`, removing any day removes the whole span, 92-day safety cap); for recurring events it becomes the series end. Frequencies now: daily / weekly / fortnightly / monthly / **quarterly** (same day-of-month every 3rd month) / **yearly**. Submit is blocked when finish < start.
2. **Roster shifts have duration types**, per the owner: "sometimes shifts can be permanent or sometimes can be adhoc for like 1-2 months". Add-shift offers One-off / **Ongoing (permanent)** / **Temporary** (end date required, `min` = start). Patterns (`ShiftPattern`: kind, startDate, endDate?, weekday set) persist in attendance-store and MATERIALISE into concrete `RosterShift`s for the visible week (`patternOccurrencesForWeek`, ids `pattern@date`), so every existing consumer (dashboard, kiosk, portfolio, timesheets) keeps working on the same `shifts` array; day rows show Ongoing/Temp pills. Future occurrences are excluded from payable timesheets (`date <= today`).
3. **Timesheets are corrected entry-by-entry.** Each shift row in Review & approve has a ± hours field + its own remark (persisted `corrections: {delta, note}` keyed by shift id). Corrections flow: `entry.paid = max(0, actual + delta)` → week's `corrected` total → the modal's payable figure FOLLOWS the corrections until the manager types a manual override ("Use corrected"/"Use rostered" shortcuts). CSV export keeps the approved_h + remarks columns.
4. **Consumables search + product thumbnails.** Search bars on the order modal, the allowed-list manager and the page's stock table. Admins upload a product photo per catalogue item (camera button → file picker → canvas-compressed ~320px data-URL via the existing `compressImage`, stored on the item in `consumables-store`); the thumbnail replaces the icon tile everywhere. Backend note: at the consumables backend stage images move to Supabase Storage with the row keeping a path, not a data-URL.
5. **Radix lesson made a rule (real bug, caught by e2e):** any dialog whose content MUTATES its own store while open must be a CONTROLLED dialog with an always-mounted Root driven by `open` — uncontrolled Roots that re-render mid-open leak `body{pointer-events:none}`, stop closing on Escape and leave the page `aria-hidden`. Applied to the calendar DayDrawer (one page-level instance; day cells just set the selected key) and the consumables ManageListModal; NewOrder/ManageList search resets on reopen so a stale filter can't hide items.
Verified: wave-4 14/14 + wave-5 13/13 Playwright assertions (frequency set incl. quarterly month-skipping, 4-day span render + whole-span removal, backwards-date block, temporary-needs-end-date, ongoing pill materialisation, −0.5 h correction flowing into the payable total + footnote, order/manager/stock search, photo upload propagating to order tiles) · tokens ✓ · contrast ✓ · tsc ✓ · build ✓.

## 2026-07-17 — Consumables ordering wave 2: urgency, recurring drafts, order emails, order numbers (owner direction, verbatim tasks)
1. **Big thumbnails.** `ItemTile` gained a `size` prop; the order form uses the `lg` (5rem) tile so admin-uploaded product photos are actually legible, and the New order modal moved to the new `xl` modal size so two-across item cards keep full names. Admin still uploads/replaces photos in the Allowed list manager (md tiles there).
2. **Urgency.** Orders carry `standard | urgent`. Urgent is a segmented choice on the form (with a "genuine run-outs" hint), flags the queue card with a critical pill and SORTS the approval queue urgent-first.
3. **Recurring orders + drafts.** Orders can repeat weekly/fortnightly/monthly/quarterly. A recurring order is either SUBMITTED now (queue card shows "Repeats …") or SAVED AS A RECURRING DRAFT: drafts live in their own section with the next-raise date, a Submit now button and Delete; `ensureSeed` auto-raises a copy into the approval queue whenever a draft's `nextRun` has passed (the client-side stand-in for the Inngest schedule at Stage 2 — same row shape, the job just moves server-side). Raised copies note their source draft; the draft's `nextRun` advances by its frequency.
4. **Order emails.** The building keeps a PERMANENT recipient list (seeded orders@foctcleaning + buildingmanager@auroraoncollins, managed as chips in the Allowed list modal); the order form shows those and takes per-order EXTRA emails (regex-validated chips). Every order snapshots `sendTo` at creation and the queue card states "On approval, emails to …". Actual sending goes through `notify()` when the consumables backend lands — nothing is simulated as sent today.
5. **Order numbers.** `peekNextOrderId` shows the CO-number ON the form before submitting (owner: "what is order number for this request"); the same helper now issues ids in `createOrder`/draft raises, so the number the requester sees is the number the queue shows.
Verified: wave-5 suite now 23/23 (order number pre-shown, permanent + extra email chips, urgent hint, draft → drafts section → Submit now → queue card carrying Urgent + Repeats + emails, recipient added in the manager) + wave-4 14/14 · tokens ✓ · tsc ✓ · build ✓.

## 2026-07-17 — Residents module shipped (owner direction — deviation from the "coming soon only" rule, recorded per CLAUDE.md rule 5/7)
The owner explicitly ordered the ResidentRequests shell promoted to a functional **Residents** module ("Add one more module in this for resident… enrolling residents, their details, emergency details, next of kin, access credentials, issues or complaints, warnings history, parcels… hard rubbish, amenities like movie karaoke, maintenance requests, guests"). This is an owner-directed scope addition in the same class as Service Desk (2026-07-06) and Scope (2026-07-10) — not a casual ask — so it ships as a working client-side module.
- **Surface:** `/residents` under Concierge in the nav (replacing the disabled "Resident requests" entry; Parcels stays coming-soon). Directory table (search + owner/tenant filter, warning + lost-fob + awaiting-parcel flags) → one controlled page-level DRAWER per resident with tabs Profile / Access / History / Parcels / Bookings / Requests. Enrolment is an xl modal: identity, residency type, contacts, move-in, parking/vehicle/pets, medical note, REQUIRED emergency contact + optional next of kin; floor auto-derives from a 4-digit apartment; the first fob is issued automatically.
- **Data (`residents-store.ts`, `foct-residents-v1`):** Resident (emergency contacts incl. NOK, medicalNote, parkingBay/storageCage/vehicle/pets, status incl. moving-out), AccessCredential (fob/swipe/garage-remote/mailbox-key · active/lost/returned with mark-lost → reactivate flows), ResidentHistoryEntry (note/complaint/incident/warning — warnings render critical and roll up to the directory Record column), AmenityBooking, ResidentRequest (RR-refs via `nextRequestRef`).
- **Parcels are NOT duplicated:** the drawer reads the existing concierge parcel register by apartment and can mark collected — one source of truth across both modules; seeds share apartments (2704/1109/3302/0806).
- **Amenities (fixed demo set):** Cinema room, Karaoke lounge, BBQ terrace, Private dining, Pool & spa hire, Gym PT room — with location + capacity; bookings are requested from the resident record (pending) and CONFIRMED/DECLINED by the manager in the page-level section, with an over-capacity warning.
- **Requests:** hard-rubbish / maintenance / guest-access / move-booking / other, lodged from the drawer with preferred date → page queue with Schedule / Mark done / Decline.
- **Isolation intent (Stage 2):** resident records belong to concierge/BM org × building; cleaning org never reads them; owner org gets counts only; history writes audit-logged. Recorded in the store header so the RLS migration mirrors it.
- **Component fix:** `Select` now accepts + forwards `aria-label` (label-less inline selects were unnamed for AT).
Verified: new wave-6 suite 17/17 (nav link, search scoping, floor auto-fill, enrol validation gate, NOK display, mark-lost flow, warning logging + directory rollup, shared parcel collect, booking pending → confirm, RR request → schedule) + wave-5 23/23 + wave-4 14/14 · tokens ✓ · contrast (37×13) ✓ · tsc ✓ · build ✓ (25 routes).

## 2026-07-17 — Typography: Lato becomes the product face (owner direction, supersedes the 2026-07-07 system-stack decision)
Owner (with FOTA Web + Zendesk Analytics references): "Use lato, rosario font. hubot sans… replace the font overall choose one of the above. Make less clumsy for clean and easy to read."
- **Hubot Sans was trialled first** (only one of the three already in the approved library) and rejected on the owner's own brief: its barred zeros are baked into the glyphs (no `zero`/ss feature disables them — verified empirically), so every date/time/id rendered like "2Ø26-Ø7-17". It stays vendored as a Theme Builder alternate.
- **Lato ships as display+body** via the `lato-font` 3.0 npm package (SIL OFL, bundleable) — the FULL family, so the 500 (medium) and 600 (semibold) weights the hierarchy depends on are real files, not browser-synthesised (Google Fonts/fontsource only carry 400/700/900). Stacks: `--font-stack-display/body: var(--font-lato), Lato, <system stack>`. Barlow numerals and Roboto Mono are unchanged. Lato is hereby ADDED to the approved library (owner-directed); default Theme Builder slots now lato/lato/roboto-mono.
- **"Less clumsy" pass:** page titles calmed from 36px display to 24px semibold (the references' scale); `--fs-title-1` 28→24px, `--fs-title-2` 22→20px (modals/sections follow); PageHeader spacing tightened (pb-10→pb-8). Metric numbers keep the big Barlow display size — "big numbers get big & thick" still stands.
Verified: h1 computed font = Lato on a live render · full e2e regression 17/17 + 23/23 + 14/14 · tokens ✓ · contrast unaffected (colour tokens untouched) · tsc ✓ · build ✓.

## 2026-07-17 — Senior design audit executed: foundation + core components + workflow quick-wins (owner directive, verbatim spec in the work order)
The owner delivered a full UI/UX audit ("quieter but sharper"; Linear/Notion/Stripe/Deputy benchmark). Executed in the directive's own priority order:
- **P1 Typography — INTER throughout** (audit §5; supersedes the same-day Lato pick AND the Barlow-numerals decision: numerals are now Inter with tabular figures; Roboto Mono only for genuine mono contexts; Lato + Hubot Sans stay as Theme Builder alternates). New scale: page titles 26px/600, sections+modals 19px/600, card metrics 32px/700, body 14/20, table body 13px, labels/table headers 12px.
- **P1 Colour — Nature retuned to the audit palette**: canvas #F7F7F4, WHITE cards with #E1E5DE hairline + the audit's crisp `0 1px 2px rgb(20 28 20/.04)` shadow (big diffuse sage shadows gone), text #20241F/#626A61, primary green #617552 (hover #506344, soft #EAF1E5), semantic pairs per the audit's table (muted text + strong border deepened to hold AA/3:1 — verified by the 37×13 contrast gate). Nature's full-pill controls and borderless tiles retired: 12px controls / 16px cards.
- **P1 Sidebar (§7)**: 240px, 14px labels, inactive items at full sidebar-fg contrast, icons 16px at 70% weight, active = SOFT green + dark-green text + 3px left indicator (solid dark pill retired), lock icons dropped — Soon/Pro are single quiet outline badges, disabled items 60%.
- **P2 Components**: buttons 40px/32px; table headers 12px semibold uppercase at secondary contrast, 56px rows, numeric cells = tabular Inter (not mono); KPI cards tightened (p-5, 36px icon well, gap-3.5) ≈20% shorter.
- **P3 Workflows**: **decimal hours eliminated from the UI** (§5/§20) — new `fmtHM`/`fmtDeltaHM` render "18h 16m", "+16 min", "−2h 50m", approve button says "Approve 19h 31m"; CSV keeps decimals for payroll systems. **State-specific actions** (§11): Ready → Approve (direct) + Review, Needs review → Review, Approved → figure ("Review & approve everywhere" retired). Service-desk table: issue TITLE primary, ref secondary muted (§16). Residents action reads "Open resident" (§21).
- **Deferred (recorded, not yet done)**: dev-only menu for LOCAL-DEMO/Reset-demo chips (§8), collapsible nav groups, sticky table headers, list-view calendar, mobile record-cards (§23), per-status filter counts on every list page (§14).
Verified: tokens ✓ · contrast 37×13 ✓ · tsc ✓ · build ✓ · full e2e regression 23/23 + 14/14 + 17/17.

## 2026-07-17 — Audit completion wave: no deferred items left (owner: "dont deferr lets finish it now")
- **Dev menu (§8):** new `DevMenu` component (wrench button → popover). The service-desk header no longer shows LOCAL-DEMO/Reset-demo — both live inside Developer options.
- **Collapsible nav groups (§7):** sidebar section titles toggle their group (chevron, persisted in `foct-sidebar-groups`); a closed group never hides the page you're currently on.
- **Sticky table headers (§11):** `Table sticky` prop — 65vh cap + pinned thead on the four long tables (tickets, timesheets, residents directory, stock).
- **Calendar list view (§18):** Month/List segmented toggle; List groups the month's events by day (today highlighted, tap opens the day drawer) — the category filter chips with counts already existed and drive both views.
- **Mobile record cards (§23):** at <768px the timesheets, service-desk and residents tables swap for stacked record cards (status pill, key figures, state-specific actions incl. Approve/Review and Open resident); desktop keeps the full tables.
- **Filter counts (§14):** timesheet status filters (All n / Needs review n / Approved n), residents residency filters, and stock category chips all show live counts; service desk + calendar already had them.
Verified: tokens ✓ · contrast 37×13 ✓ · tsc ✓ · build ✓ · suites 14/14 + 23/23 + 17/17 + NEW wave-7 16/16 (dev menu open/close, header free of demo controls, all three count sets, computed sticky thead, group collapse/restore, list-view day groups + month return, 390px record-card behaviour on all three pages).

## 2026-07-18 — Ambient Audio module REGISTERED (owner directive: "register now, build post-Stage 9")
Scheduled background music for reception/lobby via paired browser player devices. Registered as a coming-soon shell ONLY: nav entry (Concierge → "Ambient audio · Soon"), module card on /modules (with the OneMusic Australia licensing disclaimer), roadmap radar entry, and the full spec preserved verbatim in `docs/modules/AMBIENT_AUDIO_PRD.md` (sources: org-scoped uploads + AU radio stream catalogue; tables incl. audio_schedules/audio_playback_log; /player/:deviceToken fullscreen route with Web-Audio crossfade, autoplay-gesture gate, stream fallback and Cache-Storage resilience; concierge device/playlist/week-grid UI; RLS via the standard can() model). Two hard rules recorded at the owner's insistence: **no Spotify/YouTube integration ever added casually** (their terms prohibit commercial background playback) and the **OneMusic Australia licence responsibility stays with the venue** (disclaimer ships on the module page). Zero functional code until the owner opens the build stage — CLAUDE.md rule 7 applies.

## 2026-07-18 — Cameras & doors on the dashboard (owner directive: local RTSP viewing + door soft-triggers)
1. **RTSP architecture:** browsers can't play RTSP; the supported path is a LOCAL gateway on the concierge PC — **go2rtc** (free single binary) pulls RTSP (Hanwha/Hikvision/Dahua/Akuvox URL patterns documented) and the dashboard embeds `<gateway>/stream.html?src=<name>` per camera. Gateway URL + camera list are user config (Configure dialog, `security-store`), never code. Tiles show a calm "Gateway offline" state with a setup pointer when go2rtc isn't running — the panel never looks broken. Full Windows setup guide: `docs/SECURITY_CAMERAS_DOORS.md`.
2. **Door soft-triggers:** big icon toggle buttons (door/roller/gate/dock icons + lock badge), Locked/Unlocked pills, optional per-door AUTO-RELOCK countdown, and an on-screen security log (who/what/when, capped 50). Two modes per door: **demo** (state flips on screen only — default) and **http** (also fires the controller's LAN trigger URL via `/api/security/door`).
3. **Safety rails on the trigger route:** does NOTHING unless `SECURITY_TRIGGERS_ENABLED=1` is set on the machine (403 otherwise — verified); accepts only PRIVATE-network hosts (localhost/RFC1918/.local) so it can't be used as a general proxy; 3 s timeout. Stage 2 gates it behind `can('security','doors','trigger')` and moves the log into audit_logs — recorded in the route header.
4. This REPLACES the dashboard's "Live camera wall — Requires Automation Pro" banner: live viewing + soft triggers are now core dashboard features at the owner's direction; camera ANALYTICS (presence verification etc.) remains the Automation Pro module.
Verified: new wave-8 suite 7/7 (banner gone, offline tiles ×3, unlock → warning pill + log + toast, relock, configure adds a camera that appears as a tile) + full regression 14/14 + 23/23 + 17/17 + 16/16 · tokens ✓ · tsc ✓ · build ✓ (route confirmed 403 without the env flag).

## 2026-07-18 — Roster period clarity + kiosk selfie sign-in (owner directive)
1. **Roster views say their period.** The segmented options are now "Today / This week / Timeline · today", and the timeline card carries a header with today's full date, a Live pill, and copy pointing future days at This week and monthly planning at the Calendar (the owner asked "is the timeline day, week or month?" — now it answers itself).
2. **Kiosk name search.** Cleaners type part of their name and tap it from matching chips (staffDirectory incl. manager-added cleaners); the PIN then must belong to the selected person ("That PIN doesn't match Marcus Chen") — direct PIN-only entry still works.
3. **3-2-1 selfie on every sign in/out.** After a valid PIN the kiosk records the event IMMEDIATELY (time integrity — the stamp is button-press time, not post-countdown), then opens the front camera, counts 3-2-1, captures a 320px JPEG (~15 KB) and attaches it to that event (`AttendanceEvent.selfie`, new `attachSelfie` action). The done screen shows the photo; the roster Today table shows in/out thumbnails. Camera missing/denied or Skip → sign-in stands without a photo, stated plainly. Selfies are stripped from localStorage after 48 h (quota); Stage 2 moves them to org-scoped Supabase Storage with the event row keeping a path.
4. **iPad note (recorded for deployment):** Safari only allows camera access on secure origins — the permanent cleaners-room iPad needs the app served over HTTPS (the Stage 2 hosted deployment, or `next dev --experimental-https` for LAN testing). Recorded in the kiosk component header.
Verified: new wave-9 suite 7/7 under Chromium's fake-camera flags (period labels + dated timeline header, name search → greeting, cross-person PIN rejection, countdown render, "with photo" done screen with the captured image, thumbnail landing on the roster) + full regression 14/14 + 23/23 + 17/17 + 16/16 + 7/7 · tokens ✓ · tsc ✓ · build ✓.

## 2026-07-21 — Dashboard security refinements + ticket search + generated PINs (owner feedback)
- **Cameras & doors moved up a row** — now directly under the hero, above the stat row (the owner's first-glance surface).
- **Doors read like a real access-control panel:** one row per door with an icon well and a proper TOGGLE SWITCH (green = secured, amber = unlocked with the auto-relock countdown in the caption) — replacing the big pressable tiles. `role="switch"`, same aria-labels, same store/toasts/http-trigger behaviour.
- **Security-log card removed from the dashboard** (it grew and shifted the layout). Events are still recorded in the store for the Stage 2 audit_logs migration — the UI just doesn't render a feed.
- **Live activity card removed** from the dashboard (owner: "remove live activity streaming"); Today's shifts now takes the full width.
- **Ticket desk search** (owner: "search area or add search filters"): SearchInput in the FilterBar matching ref, category/title, AREA/level, reporter and assignee — composes with the existing view chips and status select.
- **Kiosk PINs are now SYSTEM-GENERATED** (owner: duplicates possible if admins pick): `addStaff` generates a unique non-trivial 4-digit PIN when none supplied; the Add-cleaner form drops the PIN field and the success toast shows the PIN once — "tell them privately". Name search remains the primary kiosk flow. (Q&A recorded: kiosk sign-ins DO feed timesheets — rows derive from kiosk events vs roster.)
Verified: new checks 9/9 (panel above stat row, no Live activity, switches with secured captions, unlock→amber caption→relock, no log card, ticket search by area narrowing to one row + reporter search) + 2/2 (no PIN field, generated PIN toast) + full regression 23/23 + 14/14 + 17/17 + 16/16 + 7/7 · tokens ✓ · tsc ✓ · build ✓.
